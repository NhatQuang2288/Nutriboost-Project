# Trên máy bình thường, dòng này là đủ.
# Trong môi trường DSH sandbox, biến `npm_config_cache` được set sẵn trong env
# và env THẮNG .npmrc — khi đó phải thêm cờ CLI (cờ CLI thắng env):
#     npm install --cache ./.npm-cache
cache=./.npm-cache

fund=false
audit=false
save-exact=true
